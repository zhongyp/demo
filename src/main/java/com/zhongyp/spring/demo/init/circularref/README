## 循环依赖问题测试

### 什么是循环依赖问题？

例如：bean A依赖bean B，bean B又依赖bean A，导致循环依赖，这就是常见的循环依赖问题。
 
### 循环依赖的产生原因？

假如bean A在构造器中依赖bean B，bean B 同样在构造器中依赖bean A就会产生循环依赖。因为既构建不了A也构建不了B。


### 解决方案？

如果有循环依赖的情况，一般是因为使用构造注入导致的。如果需要相互依赖，建议使用设值注入。

### FAQ

如果在初始化bean的afterPropertiesSet方法中依赖其他的bean也需要注意循环依赖问题。