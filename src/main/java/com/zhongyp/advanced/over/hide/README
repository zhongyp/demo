## 向上强制类型转换


### 正如A.java中的例子

子类向上强制类型转换后，如果这个实例没有再向下进行强制类型转换，则只能输出子类的@Override的方法，即使没有添加@Override注解，但只要是重写的父类方法就可以，如例子中的testAccess方法。

除了子类的重写父类的方法外，其他的方法、变量均不能访问。

### FAQ

为什么向上强制类型转换后，再向下强制类型不会报错。如:

`A a = new B(); B b = (B)a;`这时因为向上强制类型转换,会初始化B的实例，并且a会引用实例B，只不过功能上会限制B的访问，所以再强制向下转换时就不会报错。

但是如果直接声明的本来就是`A a = new A()`，则只要向下进行强制类型转换一定会抛出异常。